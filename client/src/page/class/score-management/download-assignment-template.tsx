export default function DownloadAssignmentTemplate() {
   //    const [excelData, setExcelData] = useState<any>();
   // const handleFileChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
   //     if (e.target.files) {
   //        try {
   //           const file = e.target.files[0];

   //           Papa.parse<string[]>(file, {
   //              worker: true, // use a web worker so that the page doesn't hang up
   //              complete({ data }) {
   //                 setExcelData(data);
   //              },
   //           });
   //        } catch (error) {
   //           console.error(error);
   //        }
   //     }
   //  };
   return (
      <>
         {/* <CSVLink filename="my-file.csv">
            <Tooltip title="Download the assignment template">
               <Avatar sx={{ bgcolor: "green", cursor: "pointer" }}>
                  <FontAwesomeIcon icon={faFileExcel} />
               </Avatar>
            </Tooltip>
         </CSVLink> */}
      </>
   );
}
